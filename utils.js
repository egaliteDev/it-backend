/**
 * ! Удаление дубликатов из массива
 */

{
let chars = ['A', 'B', 'A', 'C', 'B'];
let uniqueChars = [...new Set(chars)]
}

/**
 * ! Длина Set
 */
{
let chars = ['A', 'B', 'A', 'C', 'B'];
let uniqueCharsLength = [...new Set(chars)].size
}
